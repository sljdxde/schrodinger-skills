# 多线程下载踩坑手册（troubleshooting）

本文件沉淀真实环境下用自写多线程下载器拉大文件（Windows ISO、IntelliJ IDEA、麒麟 ARM64 ISO）时遇到的坑，以及 `mtd.py` 现在的对策。改下载引擎前先读一遍，避免把已修的坑再踩一遍。

---

## 坑一：Python urllib 被 Cloudflare CDN 拦截

**现象**：`python3 mtd.py "https://download.jetbrains.com/..."` 报 `HTTP Error 404`，但 `curl` 正常。

**根因**：JetBrains 的 CDN 后端是 **Cloudflare China**（响应头 `cf-ray` / `cf-cache-status: HIT`）。Cloudflare 按 TLS 指纹（JA3）拦截非浏览器客户端；Python `urllib`/`http.client`（macOS SecureTransport）的指纹不在白名单，直接被 404。

**对策（已实现）**：彻底不用 urllib，所有 HTTP 请求走**系统 `curl` 子进程**。

---

## 坑二：CDN 鉴权 URL 过期导致把 HTML 错误页当文件写入

**现象**：JetBrains IDEA 下载完 SHA256 不匹配，macOS 报"磁盘映像已损坏"。

**根因**：国内用户被 302 重定向到 Cloudflare China CDN，带 `Expires/Signature/Key-Pair-Id` 的鉴权 URL 有时效性。脚本若开始下载前取一次 CDN URL、多线程共用，部分线程的 Range 请求被 CDN 拒绝，返回 **HTML 错误页**；老脚本没开 `-f`，错误页被当文件内容写盘。

**对策（已实现）**：
- curl 带 `-f`：HTTP 错误码（≥400）返回非零退出码，错误页不会被写入。
- 每个线程传入**原始 URL + `-L`**，各自独立跟随重定向拿新的 CDN 签名 URL，不共用一次性的鉴权 URL。

---

## 坑三：CDN Range 请求返回全零数据

**现象**：麒麟 ARM64 ISO 下载完文件大小精确匹配，但 SHA256 完全不对；手动 `xxd` 发现 Range 返回的内容全是 `00`。

**根因**：部分国产 CDN（如 `iso.kylinos.cn`）的 HTTP Range 实现有缺陷——HEAD 声称支持 Range、Range GET 返回包含零填充的 body、Content-Length 仍正确、curl 退出码 0。

**对策（已实现）**：探测阶段实测下载一块数据（优先文件**中部**，避免正常文件开头恰好是零区），若 **>95% 为零**则判定该 CDN 的 Range 实现有缺陷，自动退化为**单线程整文件下载**（整文件下载不受 Range 缺陷影响）。

---

## 坑四（新）：CDN 单连接限速 + 大块 Range 挂死（间歇性）

**现象**：麒麟 ISO 单线程约每 10 分钟断流一次且越恢复越慢（330MB→19MB→11MB→挂死）；16 线程多线程一次就死（curl 退出码 56）。

**排查（Range 大小梯度测试）**：

| Range 大小 | 20 秒内下载量 | 结论 |
|-----------|------------|------|
| 5MB | 5MB（~0.8MB/s） | ✅ 正常 |
| 10MB | 7~8MB（0.4MB/s） | ⚠️ 开始限速 |
| 100MB | 0MB（挂死） | ❌ 直接挂死 |
| 1.2GB（旧 mtd.py 默认分块） | 卡死 | ❌ 挂死 |

**根因**：该 CDN 对**每个连接**限速 ~0.5MB/s，且 Range 请求越大越容易触发挂死（服务器停止发数据但连接不断开，curl 傻等超时）。旧 `mtd.py` 按 `总大小/线程数` 给每个线程切一大块（麒麟下 ≈1.2GB/块），挂死一块就阻塞整个线程池。

**附带发现**：该 CDN 故障是**间歇性**的——上午测 Range 返回全零（坑三），下午测数据正常。遇到"多线程失败"先重测一次当前状态，别急着换方案。

**对策（已实现 · 核心改进）**：多线程改为**固定 5MB 小分块 × 高并发线程池**，每块独立下载、独立重试（默认 5 次），**块级容错**——单块失败不影响其它块。小分块保证在限速生效前就下完，从根本上规避挂死。可用 `--chunk 2` 进一步调小分块、`--max-retry 8` 提高重试上限。

实测：从单线程 0.3MB/s 提升到 2.5MB/s（稳定），无卡死。

---

## 坑五（新）：下载中断与断点续传（不稳定 CDN 兜底）

**现象**：单线程下载时 CDN 断流，curl 进程 CPU 0% 傻等（`--max-time` 设太长），进度长期停滞。

**对策（已实现）**：
- **单线程**：`curl -C - --max-time 600 --connect-timeout 30` 续传循环，限制单次连接时长防无限傻等，断了自动重连直到 ≥ 总大小。用法：`python mtd.py <URL> --resume`（单线程路径自动走续传循环）。
- **多线程**：已完成块记录在 `<输出>.mtd-progress`，中断后同一命令加 `--resume` 只补下未完成块，不重下已完成的。

注意：`pgrep -f` 可能匹配到 zsh 包装进程导致监控误判"完成"，盯进度用日志里的百分比为准。

---

## 坑六：macOS Gatekeeper 隔离标记

**现象**：下载完成的 DMG 双击提示"磁盘映像已损坏"。

**根因**：macOS 为所有从网络下载的文件添加 `com.apple.quarantine` 隔离属性，对非 App Store 签名的 DMG 有时无法正确验证，直接报"损坏"。

**对策（已实现）**：下载完成后自动 `xattr -c` 清除隔离标记；可用 `--no-clear-quarantine` 关闭。

---

## 坑七（新）：WAF / 限流下不会自动回退单线程（已修复）

**现象**：遇到带 CloudWAF 的镜像（如华为云 `mirrors.huaweicloud.com` 返回 418、或限流 429/503）时，多线程模式下每个分块都被拦，旧版只会把每块重试 5 次 × 16 线程 ≈ 80 个请求狂轰，最后全部分块失败、整体删除文件，**从不回退单线程**；且高并发打 WAF 极易触发 IP 拉黑。

**根因**：旧版只在「探测阶段识别 Range 缺陷（全零/撒谎 Range）」时才退单线程；对 WAF/限流类响应（401/403/407/418/429/503）完全没有识别，分块下载失败只机械重试，没有「停手 + 换单线程」的决策。

**对策（已实现 · 核心改进）**：
- **探测阶段识别**：`get_remote_info` 现在解析 HEAD 与 Range 探测的 HTTP 状态码，命中 WAF/限流码（尤其 418/429）直接判 `hostile`，**不发任何后续 Range 请求**，从一开始走单线程，避免并发爆发。
- **分块下载阶段识别**：`_download_block` 通过 `curl -D` 捕获每个分块的最终状态码；一旦命中 hostile 码立即上报、**取消其余未开始的任务**，不再对 WAF 反复重试。
- **自动回退单线程**：`_download_multi` 在命中 hostile / 服务器无视 Range（返回越界内容）时，停止并发、清理半成品，调用单线程整文件下载（整文件下载不受 Range 限制，且单连接不易触发 WAF）。
- **瞬时错误兜底**：仅网络抖动 / 5xx 网关类错误时，先单线程逐个补下失败块，仍失败再回退整文件单线程，兼顾速度与安全。

实测（本地模拟服务器）：`/waf`(418) 仅发 **1 个** Range 探测请求即回退单线程成功（旧版会发 ~80 个）；`/conclimit`(并发超限 503) 在分块中检测到 503 立刻中止并回退单线程成功。

---

## 工具演进

| 版本 | 引擎 | 解决 |
|------|------|------|
| v1 | Python urllib | —（被 Cloudflare 拦，弃用） |
| v2 | curl subprocess | 坑一 Cloudflare 拦截 |
| v3 | curl subprocess | 坑二 鉴权 URL 过期（`-Lf` + 原 URL） |
| v4 | curl subprocess | 坑三 全零 Range（预检测 + 退化单线程） |
| mtd-download 1.1 | curl + 线程池 | 坑四 小分块高并发 + 块级容错；坑五 断点续传；坑六 Gatekeeper 清除；**坑七 WAF/限流(418/429/503)自动回退单线程** |
| **mtd-download 1.2** | curl + 线程池 + 完整性校验 | **坑八 零空洞扫描 + 格式校验（绝不交付满尺寸坏文件）；坑九 --resume 零空洞自愈；坑十 透明代理截断 + --noproxy/--mirror ghproxy 直连；坑十一 GitHub blob 自动转 raw；坑十二 前台长下载 SIGKILL 提示** |

---

## 坑八（P0）：只比文件大小放过内部零空洞（已修复 · 最致命）

**现象**：多线程下载 GitHub 上 12.8MB PDF，文件大小 `13425282` 与服务器 `content-length` **完全一致**，但 Preview 打开报"文件损坏"。逐字节扫描发现内部有 **4 段约 2MB 的零字节空洞**（位于 7%/30%/54%/75% 处）——是多线程被中断、部分分块没写进去只留预分配的空档。旧版只比对最终大小，把**满尺寸但内部空洞的坏文件当成功交付**。

**根因**：合并后只校验 `st_size == total_size`，不检查内部内容。预分配 + 分块 `os.pwrite` 在进程被杀时不回滚，留下零空洞。

**对策（已实现 · 1.2 核心改进）**：
- 下载完成后**逐字节扫描大段连续零字节**（两阶段：64KB 块级 `count` 快速定位候选 → 局部逐字节确定真实边界，避免低估空洞导致边缘漏检），发现即判失败并**自动补下对应分块**。
- 合并后做**格式校验**：PDF 检查 `%PDF` 头 + `%%EOF` 尾；通用调用 `file` 探测是否下成了 HTML 错误页。
- 任何形式的「大小对但内容可疑」都**不能**算成功，必须删除半成品或触发补下。

---

## 坑九（P1）：进程被 SIGKILL 后无自愈（已修复）

**现象**：多线程在最后校验/清理阶段被 SIGKILL（exit 137），输出文件残留零空洞。旧版 `--resume` 依赖 `<输出>.mtd-progress` 进度文件——若进程在写进度前被杀，重跑无法识别「哪些块是空洞」，自愈失败。

**对策（已实现）**：`--resume` 重跑时**不仅读进度文件，还主动扫描已存在文件里的大零空洞并视为未完成块**，把被零空洞覆盖的分块重新标记为未完成并补下。即使进度文件缺失也能自愈。补下后会再次复核，仍有空洞则回退整文件单线程下载（覆盖半成品）。

---

## 坑十（P2/P3）：透明代理截断大响应体 + 无代理绕过（已修复）

**现象**：沙箱注入 `HTTPS_PROXY` 且 `transparent_proxy: true`，该代理对 **>4.2MB** 的海外 CDN 响应体做截断/zlib 包裹损坏。默认 `chunk=5MB` 单块 >4.2MB 走代理时**每块都会被截坏**；且 skill 直接吃环境代理，用户只能手动 `unset` 再跑。

**对策（已实现）**：
- 默认分块**自动降到 2MB**（低于 4.2MB 截断阈值），代理环境会打印提示。
- 新增 `--noproxy`：下载时清除所有代理变量（`HTTP_PROXY/HTTPS_PROXY/...` + `NO_PROXY='*'`）直连。
- 新增 `--mirror ghproxy`：把 URL 包成 `https://ghproxy.net/<URL>` 直连（内置清除代理变量），绕过透明代理。GitHub 大文件推荐 `--mirror ghproxy`。

---

## 坑十一（P4）：GitHub blob 链接未归一化（已修复）

**现象**：用户给 `github.com/<owner>/<repo>/blob/<ref>/<path>` 页面链接，直接 `curl` 返回的是 **HTML 页面**而非文件本体。

**对策（已实现）**：探测前自动将 `github.com/.../blob/...` 改写为 `https://raw.githubusercontent.com/<owner>/<repo>/<ref>/<path>`（并去掉 `?token=` 等查询参数）；再结合 `--mirror ghproxy` 可走镜像直连。即使归一化后仍被代理截断，格式校验（检测到 HTML）也会判失败拦截，不会交付错文件。

---

## 坑十二（P5）：前台长下载必被 SIGKILL（已缓解）

**现象**：沙箱前台任何长下载（单连接续传 / 多线程）约 3 轮后被杀（exit 137 = SIGKILL，非超时）；后台（`run_in_background`）模式可稳定跑完。

**对策（已实现 + 文档提醒）**：
- 文档明确提示：**大文件下载建议走后台**；且即使前台被中断，重跑 `--resume` 也能自愈（坑九）。
- 单线程路径用 `curl -C -` 续传循环，内存占用极小、天然断点续传，适合长任务。
- 代理环境 + 大文件会额外打印前台被杀与后台化的提醒。

## 各文件 CDN 经验参考

| 文件 | CDN | Range 可用 | 建议方式 |
|------|------|------|------|
| 微软 Windows ISO | Azure CDN | ✅ | 多线程（默认） |
| JetBrains IDEA | Cloudflare China | ✅（需 `-L` 重定向） | 多线程（默认） |
| 麒麟 ISO | iso.kylinos.cn | ⚠️ 间歇性异常（全零/限速挂死） | `--chunk 2 --max-retry 8`；若探测判全零则自动单线程 |
| 华为云镜像 | mirrors.huaweicloud.com | ❌ 高并发 Range 被 CloudWAF 返回 418 并拉黑 IP | 探测即自动回退单线程（坑七）；切勿用多线程硬刚 WAF |
